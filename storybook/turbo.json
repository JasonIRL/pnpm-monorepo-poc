{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:storybook": {
      "dependsOn": ["^build", "^build:storybook"],
      "outputs": ["storybook-static/**"]
    }
  }
}
